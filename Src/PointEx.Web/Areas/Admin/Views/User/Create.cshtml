@model PointEx.Web.Models.UserForm

@{
    ViewBag.Title = PointExResources.CreateUserTitle;
}

<h3>@PointExResources.CreateUserTitle</h3>
@Html.BackButton("Index", "User", "Admin")
<hr />
@using (Html.BeginForm("Create", "User", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    
    @Html.Partial("_CreateEditControls")
    
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Crear Usuario"  class="btn btn-default" />
        </div>
    </div>
}
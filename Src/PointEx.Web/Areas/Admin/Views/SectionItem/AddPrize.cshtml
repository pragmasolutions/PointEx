@using Framework.Common.Extentensions
@using PagedList.Mvc
@model PointEx.Web.Models.AddPrizeModel

@{
    ViewBag.Title = "Agregar Items";
}

<h3>Agregar Premios Sección: <strong>@Model.SectionName</strong></h3>
@Html.BackButton("Index", "SectionItem", new { area = "Admin", sectionId = Model.SectionId })
<hr />

@Html.FilterButton()

<div class="spacer">
</div>

@Html.Partial("_PrizeFilters", Model)

<div class="spacer">
</div>

@Html.Partial("SelectPrizeList", Model)


@using System.Globalization
@model System.Data.Entity.Spatial.DbGeography
@if (Model != null && Model.Latitude.HasValue && Model.Longitude.HasValue)
{
    @Html.TextBox("", Model.Latitude.Value.ToString(CultureInfo.InvariantCulture) + "," + Model.Longitude.Value.ToString(CultureInfo.InvariantCulture), new { @class = "editor-for-dbgeography form-control" })
}
else
{
    @Html.TextBox("", "", new { @class = "editor-for-dbgeography form-control" })
}

@model System.Data.Entity.Spatial.DbGeography
@if (Model != null)
{
    @Html.TextBox("", Model.Latitude + "," + Model.Longitude, new { @class = "editor-for-dbgeography form-control" })
}
else
{
    @Html.TextBox("", "", new { @class = "editor-for-dbgeography form-control" })
}

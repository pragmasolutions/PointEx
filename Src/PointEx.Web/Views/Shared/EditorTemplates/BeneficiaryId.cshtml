@using Framework.Common.Web.Extensions
@using PointEx.Service
@model int?
@{
    var service = IocContainer.GetContainer().Get<IBeneficiaryService>();
    var selectList = service.GetAll().ToSelectListItem(Model, s => s.Id, s => s.Name, s => s.Id.ToString());
}

@Html.DropDownList("", selectList, "Seleccione Beneficiario", new { @class = "form-control select2-control" })
